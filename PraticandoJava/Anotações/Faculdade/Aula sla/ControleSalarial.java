public class ControleSalarial {
    public double getSalarioFinal(Funcionario f) { // Polimorfismo
        System.out.println(f.getClass().getName()); // Essa linha vai identificar o nome da classe
        return 0;
    }
}
